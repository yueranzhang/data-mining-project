import wearjp

if __name__ == "__main__":
    data = wearjp.Crawling(4)
    data.get_sets()
    set_num = 0
    for count in data.set_link_list:
        set_num += 1
        i = data.get_pic(count)
        data.download_pic(i, set_num)
